
Процедура ПередЗаписью(Отказ, Замещение)
	Для Каждого Запись Из ЭтотОбъект Цикл
		Если НЕ ЗначениеЗаполнено(Запись.Номенклатура) Тогда
			ТекстОшибки = "В строке %1 должна быть заполнена номенклатура. Запись невозоможна";
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(ТекстОшибки, Запись.НомерСтроки),,,, Отказ);
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры
