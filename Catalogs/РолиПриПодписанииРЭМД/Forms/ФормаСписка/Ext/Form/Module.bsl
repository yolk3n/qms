#Область ПрограммныйИнтерфейс

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Подсистема Архивирование
	АрхивированиеСервер.ФормаСпискаПриСозданииНаСервере(ЭтотОбъект, "Список");
	// конец Подсистема Архивирование
	
КонецПроцедуры

#КонецОбласти

#Область Архивирование

// Подсистема Архивирование
&НаКлиенте
Процедура Подключаемый_ПросмотрАрхивныхЭлементов(Команда) Экспорт
	
	Архивирование.КомандаПросмотрАрхивныхЭлементов(ЭтотОбъект, "Список", Команда);
	
КонецПроцедуры
// конец Подсистема Архивирование

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
Процедура ЗагрузитьИзНСИ(Команда)
	ОповещениеОЗакрытии_ = Новый ОписаниеОповещения("ПослеЗагрузкиИзНСИ", ЭтотОбъект);

	Параметры_ = Новый Структура();
	Параметры_.Вставить("ИмяОбъекта", "РолиПриПодписанииРЭМД"); 
	Параметры_.Вставить("ЗаголовокФормы", "Добавление ролей при подписании для РЭМД в справочник"); 
	Параметры_.Вставить("ЗагружатьИзИнтернета", Истина);
	Параметры_.Вставить("ПараметрыЗагрузки", Новый Структура("ОбновитьПринудительно", Истина));
	ОткрытьФорму("ОбщаяФорма.ЗагрузкаНСИИзКлассификатора", Параметры_, Элементы.Список, , , , ОповещениеОЗакрытии_);
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Вызывается в ОписанииОповещения после закрытия формы загрузки из справочника
&НаКлиенте
Процедура ПослеЗагрузкиИзНСИ(РезультатВызова, ПараметрыВызова) Экспорт
	
КонецПроцедуры


#КонецОбласти