
&НаСервере
Процедура ПрочитатьИзБитФинансНаСервере()
	сок_Веб.ПолучитьСписокСтатейДДС();
КонецПроцедуры

&НаКлиенте
Процедура ПрочитатьИзБитФинанс(Команда)
	ПрочитатьИзБитФинансНаСервере();
	Элементы.Список.Обновить();
КонецПроцедуры
