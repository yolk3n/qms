
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("Подписанты") Тогда
		Для каждого Подписант Из Параметры.Подписанты Цикл
			НоваяСтрока = Подписанты.Добавить();
			НоваяСтрока.Подписант = Подписант;
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Сохранить(Команда)
	
	МассивПодписантов = Новый Массив;
	Для каждого СтрокаТаблицы Из Подписанты Цикл
		Если ЗначениеЗаполнено(СтрокаТаблицы.Подписант) Тогда
			МассивПодписантов.Добавить(СтрокаТаблицы.Подписант);
		КонецЕсли;
	КонецЦикла;
	
	Закрыть(МассивПодписантов);
	
КонецПроцедуры

#КонецОбласти


